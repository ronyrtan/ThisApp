<%= form_for(@user) do |f| %>
  <% if @user.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@user.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% @user.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

    <div class="field">
      <%= f.text_field :name, :class => 'signInUpForm', :placeholder => 'name' %>
    </div>
    <div class="field">
      <%= f.text_field :email, :class => 'signInUpForm', :placeholder => 'email' %>
    </div>
    <div class="field">
      <%= f.password_field :password, :class => 'signInUpForm', :placeholder => 'password' %>
    </div>
    <div class="field">
      <%= f.password_field :password_confirmation, :class => 'signInUpForm', :placeholder => 'password confirmation' %>
    </div>
    <div class="actions">
      <%= f.submit :class => 'signInUpFormButton' %>
    </div>
<% end %>
